# backup steps/tips

# database:

ssh image
pg_dump image-a-day_production -U image-a-day | gzip > db_backup.gz

scp image:db_backup.gz .

# image files

# (on local machine)

rsync -azhP --ignore-existing image:httpdocs/image-a-day/assets/images/ assets/images