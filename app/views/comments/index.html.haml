%h2 Listing comments

%table
  %tr
    %th Text
    %th Time
    %th Icon
    %th
    %th
    %th

  - @comments.each do |comment|
    %tr
      %td= comment.text
      %td= comment.icon
      - if comment.user_id == current_user.id
        %td= link_to 'Delete', comment, :confirm => 'Are you sure?', :method => :delete

%br
