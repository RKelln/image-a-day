%h2 
  = @user.nickname
  Images

.paginate
  %p
    = will_paginate @images

- @images.each do |image|
  = render image, :size=>:thumb


-# @images.each do |image|
  %p
    = link_to image do
      %img(src="#{image.data.url(:icon)}")
    = image.description
    = image.date

.paginate
  %p
    = will_paginate @images
